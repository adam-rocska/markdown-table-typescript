import {greatestLengthPerColumn} from '!src/greatest-length-per-column';

describe('greatestLengthPerColumn', () => {
  it('returns the greatest length of each column in the given rows.', () => {
    const lengths = greatestLengthPerColumn(
      ['a', 'bb', 'ccc'],
      ['dddd', 'ee', 'f']
    );
    expect(lengths).toEqual([4, 2, 3]);
  });

  it('returns the greatest length of each column in the given rows.', () => {
    const lengths = greatestLengthPerColumn(
      ['a', 'bb', 'ccc'],
      ['dddd', 'ee', 'f']
    );
    expect(lengths).toEqual([4, 2, 3]);
  });

  it('returns the greatest length of each column in the given rows.', () => {
    const lengths = greatestLengthPerColumn(
      ['a', 'bb', 'ccc'],
      ['dddd', 'ee', 'f']
    );
    expect(lengths).toEqual([4, 2, 3]);
  });

  it('returns the greatest length of each column in the given rows.', () => {
    const lengths = greatestLengthPerColumn(
      ['a', 'bb', 'ccc'],
      ['dddd', 'ee', 'f']
    );
    expect(lengths).toEqual([4, 2, 3]);
  });

  it('returns the greatest length of each column in the given rows.', () => {
    const lengths = greatestLengthPerColumn(
      ['a', 'bb', 'ccc'],
      ['dddd', 'ee', 'f']
    );
    expect(lengths).toEqual([4, 2, 3]);
  });

  it('returns the greatest length of each column in the given rows.', () => {
    const lengths = greatestLengthPerColumn(
      ['a', 'bb', 'ccc'],
      ['dddd', 'ee', 'f']
    );
    expect(lengths).toEqual([4, 2, 3]);
  });

  it('returns the greatest length of each column in the given rows.', () => {
    const lengths = greatestLengthPerColumn(
      ['a', 'bb', 'ccc'],
      ['dddd', 'ee', 'f']
    );
    expect(lengths).toEqual([4, 2, 3]);
  });

  it('returns the greatest length of each column in the given rows.', () => {
    const lengths = greatestLengthPerColumn(
      ['a', 'bb', 'ccc'],
      ['dddd', 'ee', 'f']
    );
    expect(lengths).toEqual([4, 2, 3]);
  });
});
